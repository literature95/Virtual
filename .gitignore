# ── Dart / Flutter ──
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.flutter_tool_state
.pub-cache/
.pub/
build/
*.g.dart.tmp
local.properties

# ── Node / Vite ──
node_modules/
dist/
.vite/

# ── IDE / 系统 ──
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# ── 本地工具状态 ──
.workbuddy/
.mason-cache/
.mason_cache/
*.log
# 构建/测试的临时输出（沙箱里重定向出来的日志，不入库）
*_log.txt
build_log.txt

# ── 敏感文件 ──
.env
.env.*
*.keystore

# ── AI 生成图片临时产物（未确认不入库）──
generated-images/

# ── 卡片解析派生缓存（10 万+ JSON，不入库）──
cards-js/
